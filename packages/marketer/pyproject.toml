[tool.coverage.run]
branch = true
source = [ "app" ]

[tool.coverage.report]
exclude_lines = [ "if TYPE_CHECKING:" ]
show_missing = true

[tool.pytest.ini_options]
addopts = """
 --cov
 --cov-report html:'../../coverage/packages/marketer/html'
 --cov-report xml:'../../coverage/packages/marketer/coverage.xml'
 --html='../../reports/packages/marketer/unittests/html/index.html'
 --junitxml='../../reports/packages/marketer/unittests/junit.xml'
"""

[tool.hatch.build.targets.wheel]
packages = [ "app" ]

[tool.hatch.metadata]
allow-direct-references = true

[tool.ruff]
exclude = [ ".ruff_cache", ".svn", ".tox", ".venv", "dist" ]
line-length = 120
indent-width = 4

  [tool.ruff.lint]
  select = [ "E", "F", "UP", "B", "SIM", "I" ]
  ignore = [ ]
  fixable = [ "ALL" ]
  unfixable = [ ]

[project]
name = "play_c463_z26_rzy_mar_tech.marketer"
version = "1.0.0"
description = "Automatically generated by Nx."
requires-python = ">=3.12"
readme = "README.md"
dependencies = [
  "aws-opentelemetry-distro==0.15.0",
  "bedrock-agentcore[strands-agents]==1.3.1",
  "fastapi==0.128.5",
  "boto3==1.42.44",
  "mcp==1.26.0",
  "strands-agents==1.26.0",
  "strands-agents-evals==0.1.6",
  "strands-agents-tools==0.2.20",
  "uvicorn==0.40.0"
]

[build-system]
requires = [ "hatchling" ]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
  "fastapi[standard]==0.128.5",
  "pytest>=9.0.0",
  "pytest-cov>=7.0.0",
  "pytest-html>=4.0.0",
  "coverage>=7.0.0",
]
